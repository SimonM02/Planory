name: Push-Erinnerungen versenden

# Ruft regelmaessig die Vercel-Funktion push-cron.js auf, damit faellige
# Erinnerungen (Zahlungen, Meilensteine, Wartung, Skonto-Fristen) auch dann
# als Web-Push rausgehen, wenn der Browser/die PWA geschlossen ist.
#
# EINMALIGE EINRICHTUNG (vom Nutzer):
#   GitHub -> Repo -> Settings -> Secrets and variables -> Actions -> New secret
#     Name:  CRON_SECRET
#     Wert:  derselbe Wert wie die Environment-Variable CRON_SECRET in Vercel
#   (Wenn in Vercel KEIN CRON_SECRET gesetzt ist, laeuft es auch ohne Secret –
#    dann ist der Endpunkt aber ungeschuetzt. Empfehlung: Secret setzen.)

on:
  # PAUSIERT (03.08.2026): Der 15-Minuten-Zeitplan ist deaktiviert, bis der
  # Server-Push (APNs / Web-Push) wirklich eingerichtet ist. Vorher lief er ins
  # Leere und hat Fehler-/Abbruch-Mails verursacht. ZUM REAKTIVIEREN einfach die
  # beiden schedule-Zeilen unten wieder einkommentieren.
  # schedule:
  #   - cron: '*/15 * * * *'
  workflow_dispatch: {}   # nur noch manuelles Ausloesen zum Testen

# Nicht mehrere Laeufe gleichzeitig – der letzte gewinnt.
concurrency:
  group: push-reminders
  cancel-in-progress: false

jobs:
  send:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: push-cron aufrufen
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          # Noch kein Secret gesetzt? Dann still ueberspringen (kein Fehler, keine
          # Mail). Push wird erst aktiv, wenn CRON_SECRET als Repo-Secret gesetzt
          # ist (gleicher Wert wie in Vercel).
          if [ -z "${CRON_SECRET}" ]; then
            echo "CRON_SECRET nicht gesetzt – Push-Zeitplan wird uebersprungen."
            echo "Zum Aktivieren: GitHub -> Settings -> Secrets and variables ->"
            echo "Actions -> New repository secret -> Name CRON_SECRET (Wert wie in Vercel)."
            exit 0
          fi
          echo "Rufe push-cron auf ..."
          # -s still, -S Fehler trotzdem zeigen.
          http_code=$(curl -sS -o /tmp/resp.json -w "%{http_code}" \
            -X POST \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            -H "Content-Type: application/json" \
            https://bauplan-weld.vercel.app/api/push-cron)
          echo "HTTP-Status: ${http_code}"
          echo "Antwort:"
          cat /tmp/resp.json || true
          echo ""
          if [ "${http_code}" -ge 400 ]; then
            echo "::error::push-cron lieferte HTTP ${http_code}"
            exit 1
          fi
